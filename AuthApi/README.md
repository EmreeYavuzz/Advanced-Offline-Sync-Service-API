# AuthApi

Main project README moved to repo root:

- [../README.md](../README.md)

This file stays only as short pointer so GitHub main page and project docs do not duplicate same content.

Local development secrets should be set with `dotnet user-secrets` for `ConnectionStrings:DefaultConnection` and `Jwt:Key`.
